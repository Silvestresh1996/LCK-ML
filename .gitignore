# Secretos — nunca subir la API key
secrets_local.py
api_key.txt
.env
lck_config.py

# Artefactos de build (PyInstaller)
build/
dist/
*.spec

# Modelos entrenados / caché
*.joblib
*.pkl
# caché local de Oracle's Elixir (se re-descarga solo)
oe_*.csv
# registro personal de apuestas
bets_log.csv

# Python
__pycache__/
*.pyc
